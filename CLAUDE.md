project name: vide (vim ide)
summary:这是一个基于vim的，运行在终端中的，类似与vscode等变成软件的tui应用，关键功能是识别出在当前工作目录中的所有文件和子目录并且以树状图的形式显示出来，同时实现vim的所有操作和功能。
detailed requirement：
界面布局：左边是一条文件管理栏，采用可收起和展开子目录的设计，方便展示整个目录的完整文件结构。右边三分之二的部分是完整的原版vim软件界面。注意分割线的完整性，不要被其他内容打断、不要出现错位等情况。
功能要求：vim文本栏部分完全套用完整的原版vim，vim的源码见文件夹内的vim-master目录（注意仔细阅读vim的源码，可以更改一些代码适应本项目，阅读vim的源码的时候可以使用subagent，但是在执行主体项目的时候不要使用subagent）。文件管理栏使用可以展开和收起的树状文件管理形式（注意三角箭头的大小）同时在文件管理栏集成了实时检测文件是否改动的功能。如果文件被改动，就将最近改动的那一个文件在管理栏中进行高亮，然后自动在vim栏中打开该文件并且定位到更改点上。并且本软件在完美兼容键盘操作的情况下，还需要兼容以下鼠标直接点击操作。
tips：本软件是tui软件，兼容zsh和bash，请做好兼容性设计。本软件是为了当前agent的vibecoding情况下，可以比较直观看到当前的项目状态和文件结构和内容设计，注意交互细节
